OLTP（联机事务处理）
OLAP（联机分析处理）
这里要说明的是，OLTP的交易场景和OLAP的分析场景区别在于，前者强调高并发、单条数据简单提取和展示（增删改查），后者对并发的要求不高，但是需要打通不同的数据库，比如ERP、CRM、行为数据等等，并且能够进行批量的数据处理，也就是通常说的低并发，大批量（批处理）、面向分析（query+计算，用于制作报表）。

Oracle RAC
Oracle RAC是基于IOE架构的，所有数据用同一个EMC存储